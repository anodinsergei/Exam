{% extends 'base.html.twig' %}

{% block body %}
    <div align="center" style="background-color: #3a87ad">
    <h1>Blog creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('blog_index') }}">Back to the list</a>
        </li>
    </ul>
    </div>
{% endblock %}
